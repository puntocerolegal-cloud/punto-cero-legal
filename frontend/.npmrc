# React 19 con algunas libs cuyos peerDependencies aún declaran React 18
# (p. ej. react-day-picker@8). legacy-peer-deps permite que npm install
# resuelva sin abortar por ERESOLVE, tanto en local como en Vercel.
legacy-peer-deps=true
