# Punto Cero Legal — dependencias reales del backend (curado para PyPI/Render)

# ── Framework web ──
fastapi==0.110.1
uvicorn[standard]==0.29.0
python-multipart==0.0.9        # formularios/uploads (intake, comprobantes, webhook)
slowapi==0.1.9                 # Rate limiting (CRITICAL FIX S5.3-Finding#9)

# ── Base de datos (MongoDB) ──
motor==3.4.0                   # driver async (incluye bson)
pymongo==4.7.2

# ── Validación / modelos ──
pydantic[email]==2.7.4         # EmailStr requiere email-validator (incluido)
bleach==6.1.0                  # HTML sanitization (CRITICAL FIX S5.3-Finding#6)

# ── Configuración ──
python-dotenv==1.0.1

# ── Autenticación ──
passlib==1.7.4
bcrypt==4.0.1                  # compatible con passlib 1.7.4 (evita el bug de __about__)
python-jose[cryptography]==3.3.0   # JWT

# ── HTTP / integraciones ──
httpx==0.27.2                  # Gemini, Meta WhatsApp, MercadoPago, ip-api, tasas FX
requests==2.32.3

# ── IA ──
anthropic==0.69.0              # chatbot legal (Claude)

# ── Google Drive (backups — opcional; import perezoso) ──
google-api-python-client==2.130.0
google-auth==2.29.0
google-auth-httplib2==0.2.0
